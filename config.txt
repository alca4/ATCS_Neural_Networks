/*
* Author: Andrew Liang
* Date of Creation: 15/09/2023
* Configures model
*/

NUM_LAYERS
3                    // must satisfy: = 3


LAYER_SIZE
2,3,1                // must satisfy: amount of numbers equal to NUM_LAYERS


MODE                 // determines whether model will train or test
                     // 0: test
                     // 1: train
1                    // must satisfy: = 0 or 1

NUM_TESTS
4                    // must satisfy: = number of files under Tests/


WB                   // bound for random weights
                     // first is lower bound, second is upper bound
-1.5,1.5             // must satisfy: lb <= ub


LOAD                 // determines where weights come from
                     // 0: randomly initialize
                     // 1: load from file
0                    // must satisfy: load = 0 or 1

LAMBDA                              // learning rate
0.3                                 // must satisfy: lambda > 0

THRESHOLD_FUNCTION_TYPE             
                                    // 0: sigmoid function: 1/(1+exp(-x))
0                                   // must satisfy: function type = 0

MAX_ITERATIONS
100

LOSS_THRESHOLD                      // minimum loss model will achieve before quitting
0.2                                 // must satisfy: threshold >= 0
